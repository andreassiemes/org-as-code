# Legacy examples (v0.1 / v0.2)

Early fragments kept for history. They predate the root-unit requirement and
fail modern validation on purpose — do not use them as a starting point; use
`examples/pilot-starter/` or `examples/serve-demo/` instead.
